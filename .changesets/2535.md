- chore(deps): replace semver with verkit (#2535) by @lisa-assistant

Swap the `semver` dependency for `verkit` in `cli`, `cli-helpers`,
`create-cedar-app`, `create-cedar-rsc-app`, and
`cli-packages/storybook-vite`. verkit is a zero-dependency, ESM-first
semver implementation with the same range/comparator grammar as
node-semver. This is an internal implementation detail and shouldn't
require any changes on your end.
