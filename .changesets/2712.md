- chore(structure): rename URL_file to URL_fromFile for clarity (#2712) by @lisa-assistant

`URL_file`'s name was ambiguous next to `URL_toFile`. Renames it to
`URL_fromFile` and updates all internal call sites. Since `URL_file` is
re-exported from the package's public barrel, keeps a deprecated
`URL_fromFile as URL_file` alias for one release cycle.
