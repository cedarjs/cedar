- chore(vite): Use Vite job-path-injector plugin in buildApp (#2088) by @lisa-assistant

Integrates the existing `cedarjsJobPathInjectorPlugin` into the buildApp pipeline for API builds. This plugin handles path injection for Cedar job files and is functionally equivalent to the previous Babel plugin approach.

Part of the ongoing migration of API-side Babel plugins to Vite plugins.