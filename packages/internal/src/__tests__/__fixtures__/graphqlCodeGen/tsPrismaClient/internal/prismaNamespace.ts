// Stand-in for the `internal/prismaNamespace.ts` file emitted by Prisma's
// `prisma-client` generator when `generatedFileExtension` is "ts". Only the
// `ModelName` object is reproduced here — that's all graphql codegen reads.

export const ModelName = {
  PrismaModelOne: 'PrismaModelOne',
  PrismaModelTwo: 'PrismaModelTwo',
  Post: 'Post',
  Todo: 'Todo',
} as const
