// Stand-in for a generated Prisma client, as emitted by the legacy
// `prisma-client-js` generator: a single compiled module with
// `Prisma.ModelName` baked in, no `models.<ext>` barrel alongside it. The
// graphqlCodeGen tests point `resolveGeneratedPrismaClient` at this file so
// that `readGeneratedPrismaModelNames` imports it instead of shelling out to
// `yarn cedar prisma generate`.
export const Prisma = {
  ModelName: {
    PrismaModelOne: 'PrismaModelOne',
    PrismaModelTwo: 'PrismaModelTwo',
    Post: 'Post',
    Todo: 'Todo',
  },
}
