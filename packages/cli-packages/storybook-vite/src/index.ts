import {
  command,
  aliases,
  description,
  builder,
  handler,
} from './commands/storybook.js'

export const commands = [
  {
    command,
    aliases,
    description,
    builder,
    handler,
  },
]
