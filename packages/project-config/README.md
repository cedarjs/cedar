# Project Config

> **Warning**
>
> This is a new internal package. There are still changes we want to make, so we're marking it as experimental for now.
> **Don't depend on this directly in a Cedar project**.

This package offers functionality to parse Cedar's configuration file, cedar.toml or cedar.toml, and a convenient way of getting a Cedar project's paths.
